/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'scontent.cdninstagram.com',  // Add Instagram CDN domain here
    ],
  },
}

module.exports = nextConfig
